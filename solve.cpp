#include <bits/stdc++.h>
#define IOS ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);
#define INF 0x3f3f3f3f
using namespace std;
typedef long long ll;
const ll mod = 1e9 + 7;
ll n, q, arr[2e5 + 5], b;
vector<pair<int, int> > query[2e5 + 5];
void discretization(){

}
int main(){
    IOS
    cin >> n >> q;
    for(int i = 0;i < n;i++) cin >> arr[i];
    int l, r;
    for(int i = 0;i < n;i++) cin >> 

    return 0;
}
